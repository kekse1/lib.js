#!/usr/bin/env node.js

/*
	'prompts()' is the general function to be called when you want prompts...

	..on the one hand it'll loop through all items in the vector,
	on the other hand it'll enable AND disable the 'tty/raw/' features.. etc. :-)
 */

const THROW = true;
const VECTOR = [];//TODO/


//
//const result = prompts(VECTOR, THROW, TRUE);

