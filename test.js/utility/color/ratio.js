#!/usr/bin/env node.js

//
var float = 0.25;

//
console.eol(2);

for(var idx in color.floating)
{
	dir(color.ratio[idx]('#fff', float), 'color.ratio["' + idx + '"]("#fff", ' + float + ')');
}

console.eol(4);

