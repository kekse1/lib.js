#!/usr/bin/env node.js

throw new Error('TODO');

const cb = (... _args) => {
	//TODO/
};

const duration = time.hours(2) + time.minutes(25) + time.seconds(10) + time.milliseconds(250);
const res = at(duration, cb);

