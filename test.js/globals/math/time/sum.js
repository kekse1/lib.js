#!/usr/bin/env node.js

const duration = Math.time.hours(2) + time.minutes(25) + time.seconds(10) + time.milliseconds(250);

dir(duration, 'Math.time.hours(2) + time.minutes(25) + time.seconds(10) + time.milliseconds(250)');
console.eol(4);
const times = Math.time(duration);
dir(times.toObject(1), 'Math.time.relative(duration).toObject(1)');

