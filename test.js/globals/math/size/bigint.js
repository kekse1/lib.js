#!/usr/bin/env node.js

const size = '4 GiB';

const big = Math.size.parse(size, null, null, true);
const num = Math.size.parse(size);

dir(big, 'Math.size.parse(' + size.toString('"') + ', null, null, true)');
dir(num, 'Math.size.parse(' + size.toString('"') + ')');

