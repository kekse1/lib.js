#!/usr/bin/env node.js
//
const size = '1234567 mib';
//const bytes = Math.size.parse(size);
const tib = Math.size.render(size);
dir(tib, 'Math.size.render(' + size + ')');
console.eol(2);
//
dir(Math.size(size, false), 'Math.size(' + size.toString('"') + ')');
